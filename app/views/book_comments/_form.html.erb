      <div>
         <%= form_with model: [book,book_comment], local: false do |f| %>
         <%= f.text_area :comment, rows: '5', class: 'form-control w-100' %>
         <%= f.submit "送信" %>
         <% end %> 
      </div>